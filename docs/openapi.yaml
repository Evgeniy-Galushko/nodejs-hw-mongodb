openapi: 3.1.0
info:
  version: 1.0.0
  title: Contacts
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description: Accompanying documentation for working with the roller's contacts.
tags:
  # теги, що ви будете використовувати
servers:
  - url: http://localhost:3000
  # - url: #посилання на задеплоєний сайт
paths:
  /contacts:
    get:
      $ref: ../docs/swagger/paths/contacts/get.yaml
    post:
      $ref: ../docs/swagger/paths/contacts/post.yaml
  /contacts/{id}:
    get:
      $ref: ../docs/swagger/paths/contacts/{id}/get.yaml
    patch:
      $ref: ../docs/swagger/paths/contacts/{id}/patch.yaml
    delete:
      $ref: ../docs/swagger/paths/contacts/{id}/delete.yaml

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
